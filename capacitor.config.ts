import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.andrea.vecino',
  appName: 'music-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
